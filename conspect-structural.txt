Adapter

Для создания "более удобного интерфейса" взаимодействия с "неудобным объектом"
Критерий: нет возможности менять исходную часть

Decorator - обязан имплементировать те же интерфейсы, что и "подменяемая жертва"


Proxy - посредник
Reflection -> Method
					.getMethod(String methodName)
					.invoke()
					
Bridge
Отчет	  -->		Формат отчета
- годовой			- pdf
- квартальный   	- excel
- месячный			- html
- недельный

Отчет отчет = ...



Composite
1. Объект, которые управляет "себе подобными" - leafs
2. который имплементирует те же интерфейсы, что и "подчиненные"
	
